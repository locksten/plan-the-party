This is a React SPA for quickly trying out a simple website. Keep it focused and direct, not production-grade.
Use pnpm for dependencies and scripts. The development environment is declared with devenv for NixOS compatibility.
Run repo specific commands through `direnv exec .`
You can use `agent-browser` to interact with and inspect the running website.
